# 팀 배포 서버 연결. 가입·설문·게시글은 이 서버의 DB에 저장됩니다.
# 로컬 백엔드를 쓰려면 http://localhost:8000/api 로 변경하세요.
VITE_API_BASE_URL=https://gp-mock-inv.onrender.com/api

# Enable after verifying orders on the target mock-investment backend (server-side fill prices).
VITE_ENABLE_ORDER_SUBMISSION=false
