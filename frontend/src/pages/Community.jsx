import React from 'react';

function Community() { // 파일명에 맞춰서 이름 변경
  return (
    <div className="p-10 h-screen flex justify-center items-center font-bold text-2xl text-gray-500 bg-gray-50">
      여기는 회원가입(Register) 화면입니다.
    </div>
  );
}

export default Community; // 파일명에 맞춰서 이름 변경