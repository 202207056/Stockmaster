# 09. 개발 로드맵 및 TODO 체크리스트

> 기준일: 2026-08-25 · 남은 기간: **약 12주 (~2026.11.15 완성 목표)**
> 원안 일정표(발표 12p)는 백엔드 항목이 누락되어 있어 아래와 같이 재수립합니다.

---

## 0. 현실 인식 (2026-08-25 개정)

> ⚠️ 최초 작성 시 `gp-mock-inv/` 백엔드를 확인하지 못해 "백엔드 0줄"을 전제로 계획했습니다.
> 실제로는 백엔드가 배포까지 완료된 상태이므로, 아래와 같이 전제를 수정합니다.

| 사실 | 함의 |
|---|---|
| 백엔드 3,309줄 · 27개 엔드포인트 · Render 배포 완료 | **기반은 이미 있다.** 새로 만들 게 아니라 고치고 붙일 단계 |
| `demo.html`로 로그인~매매~랭킹까지 실제 동작 | 백엔드가 동작함을 증명하는 산출물 보유 |
| **거래 로직 치명적 결함 4건** | 무한 수익 취약점 + 랭킹 역순. **시연 전 필수 수정** |
| React 프론트 연동 0% | 최종 산출물은 React여야 함. **가장 큰 남은 작업** |
| AI 실체 없음 (외부 서버 미구축) | 4개 엔드포인트가 503 |
| 학습 시스템 0% | 발표 핵심기능인데 미착수. 단, **용어 원고 60개는 이미 있음** |
| 남은 기간 12주, 인원 6명 | 범위 조절 필요하나 **완주 가능** |

### 🔧 수정된 작업 순서 (Week 1-4 대체)

| 순서 | 작업 | 문서 |
|:--:|---|---|
| 0 | `gp-mock-inv/` git 커밋 + 저장소 통합 + `.env` 무시 | `11` §M-1 |
| 1 | **거래 로직 결함 C-1~C-4 수정 (3일)** | `11` §1 |
| 2 | KIS 시세 캐시 (TTL 5초) | `11` §H-3 / `06` §5 |
| 3 | React 프론트 ↔ 백엔드 연결 (`demo.html`을 정답지로) | `11` §6 |
| 4 | `trades` 테이블 + 실현손익 → 자산 화면 | `04` §3-3 / `07` §5 |
| 5 | 수수료·세금·장운영시간 검증 | `07` §4,§6 |
| 6 | 학습 시스템 (용어 60개 시드 → `HelpIcon` → 개념/퀴즈) | `08` §6 |
| 7 | AI 실체 (성향분석 규칙+LLM, 뉴스 요약 파이프라인) | `08` §3,§4 |
| 8 | 손실 → 학습 트리거 (체결 이벤트 연결) | `08` §6-5 |
| 9 | 폴백/데모 모드 + 데모 계정 + 리허설 | `06` §7 / `10` C-5 |

> 아래 §1의 주차별 로드맵은 이 순서를 반영해 읽으세요.
> (Week 1-2의 "FastAPI 스캐폴딩", "인증 구현" 등은 **이미 완료된 항목**입니다.)

### ✅ 범위 3단계 분류 (반드시 팀 합의할 것)

| 등급 | 내용 | 미완 시 |
|---|---|---|
| **MUST** | **거래 결함 4건 수정** · 정확한 자산 계산 · React↔백엔드 연결 · 차트 · **도움말 아이콘** · **손실→학습 트리거** · 성향분석 실체 | 프로젝트 실패 |
| **SHOULD** | 학습 개념 15개 · 뉴스 요약 파이프라인 · 관심종목 · 종목 상세 · MDD · 랭킹 정상화 · 커뮤니티 **프론트** | 발표 설득력 하락 |
| **COULD** | AI 코치 리포트 · 퀴즈/배지 · 알림 · 지정가 미체결 주문 · 시즌/리셋 · 관리자 | 없어도 방어 가능 |

> 커뮤니티는 **백엔드가 이미 완료**되어 있어 프론트 화면만 만들면 되므로 SHOULD로 올렸습니다.
> 반대로 **도움말 아이콘·손실→학습 트리거**는 발표에서 약속한 핵심 차별점이므로 MUST입니다.

> 🔴 **10월 15일까지 MUST가 끝나지 않으면 COULD를 즉시 전부 버리세요.**
> 졸업작품은 "많이 만든 것"이 아니라 **"동작하고 설명 가능한 것"** 으로 평가됩니다.

---

## 1. 주차별 로드맵 (개정)

> Week 1-2의 "FastAPI 스캐폴딩·인증·KIS 연동"은 **이미 완료**되었으므로 일정에서 제외했습니다.

### 🔧 Week 0 (8/25 ~ 8/31) — 정리 주간
| 담당 | 작업 |
|---|---|
| 팀장 | **`gp-mock-inv/` git 커밋** 🔴 최우선 · 루트 `.gitignore`에 `.env` |
| 팀장 | 저장소 이원화 정리 (루트 `backend/` 삭제) · 브랜치 전략 · 이슈 트래커 |
| 전원 | `02`·`11` 문서 회독, 범위(MUST/SHOULD/COULD) 합의 |
| BE | `11` 문서 C-1~C-4 담당 배정, 로컬에서 서버 기동 확인 |
| AI | LLM 제공사 결정 + 키 발급 + 예산 확정 |
| DB | `04` 문서 §3-1 안전장치 리비전 작성 |
| FE | Tailwind 정식 설치 + 라우팅 버그 수정 |

### 🛡️ Week 1-2 (9/1 ~ 9/14) — 거래 신뢰도 확보 [MUST]
| 담당 | 작업 | 완료 기준 (DoD) |
|---|---|---|
| BE | **C-1** 체결가를 서버가 KIS 시세로 결정 | `price:1` 주문이 시세로 체결됨 |
| BE | **C-2** `gt=0` 검증 + DB CHECK 제약 | 음수 수량 422 |
| BE | **C-3** `balance` 정리 + 총자산 계산식 | 매수해도 총자산 불변 |
| BE | **C-4** `with_for_update()` | 동시 주문 2건 중 1건 거부 |
| BE | KIS 시세 캐시(TTL 5초) + 유량 제한 | 5회 호출 시 KIS 로그 1회 |
| BE | 장 운영시간 · 호가단위 · 상하한가 검증 | 새벽 주문 거부 |
| BE | `verify=False` 환경별 분리 | 운영에서 TLS 검증 활성 |
| DB | 모델↔마이그레이션 타입 일치 + 제약·인덱스 리비전 | `autogenerate` diff 없음 |
| FE | `AuthContext` + `ProtectedRoute` + `api/client.js` | 로그인/로그아웃 동작 |
| FE | Login / Signup 실제 폼 + 백엔드 연동 | 가입 → 로그인 성공 |
| AI | **Google Docs 용어 60개 → 시드 SQL 변환** | `glossary_term` 60행 |
| AI | 설문 6문항×4선택지 확정 + 시드 | `survey_option` 24행 |

**🎯 W2 종료 목표: 취약점이 사라지고, React에서 로그인이 된다.**

### 📈 Week 3-4 (9/15 ~ 9/28) — 회계 정확성 + 화면 연결 [MUST]
| 담당 | 작업 |
|---|---|
| BE | `trades` 테이블 + 실현손익 기록 (`07` 2단계) |
| BE | 수수료·세금 정책 (`fee_policy`) |
| BE | `portfolio_service.get_summary()` — 총자산·평가손익·수익률 |
| BE | 장마감 배치 + `account_daily_snapshot` + **랭킹 정상화** |
| BE | `/api/favorites` 신규 |
| DB | 종목마스터 300종목 확대 + `market` 컬럼 |
| FE | Dashboard 실데이터 연동 (랭킹/뉴스/커뮤니티) |
| FE | Trading 페이지 주문 폼 + 종목 검색 |
| FE | Assets 페이지 실데이터 + 캔들차트(`lightweight-charts`) |
| AI | 규칙기반 성향 점수 → `ai_propensity_advice` INSERT (LLM 없이 동작) |
| AI | LLM 성향 분석 연결 + 폴백 |

**🎯 W4 종료 목표: 사고팔면 내 자산이 정확히 계산되고, React 화면에 뜬다. (최대 고비)**

### 🧠 Week 5-6 (9/29 ~ 10/12) — 차별화 기능 [MUST]
| 담당 | 작업 |
|---|---|
| BE+AI | `/api/learn/glossary` + **`HelpIcon` 전 화면 배치** ★ |
| BE+AI | `learn_concept` 15개 + Learn 페이지 |
| BE+AI | **손실 → 학습 트리거** (체결 이벤트 연결) ★ |
| BE | `MARKET_MODE` 폴백 (LIVE/REPLAY/MOCK) — 시연 안전장치 |
| BE | 지수·랭킹·호가 KIS TR 추가 |
| FE | **StockDetail 페이지 신규** · Favorites 페이지 |
| FE | 온보딩 플로우 (가입 → 설문 → 가이드 → 첫 주문) |

### 🌐 Week 7-8 (10/13 ~ 10/26) — 완성도 [SHOULD]
| 담당 | 작업 |
|---|---|
| AI | 뉴스 파이프라인 재구성 (검색API → 요약 → DB 저장 → 감성) |
| AI | AI 투자 코치 리포트 (지표 계산 + LLM) |
| FE | 커뮤니티 페이지 (백엔드는 이미 완료) |
| BE | 지정가 미체결 주문 (`07` 3단계) — 시간 되면 |
| BE | 알림 API + 헤더 벨 |
| 전원 | 체결 엔진 테스트 12종 작성 |

> **10/15 중간 점검**: MUST 완료 여부 판정 → 미완이면 COULD 전부 드롭

### 🧪 Week 9-10 (10/27 ~ 11/9) — 안정화
| 작업 |
|---|
| 통합 테스트 · 버그 수정 · 로깅/에러 핸들링 정비 |
| 성능 점검 (N+1 제거, 캐시 확인) |
| 퀴즈 30문항 + 진도/배지 |
| 사용성 테스트 (`10` 문서 C-1) 및 결과 정리 |
| 프론트 배포 + 백엔드 콜드스타트 대응 (`10` 문서 C-6) |

### 🏁 Week 11-12 (11/10 ~ 11/22) — 마무리
| 작업 |
|---|
| **데모 계정 시드** (`10` 문서 C-5) |
| 데모 시나리오 리허설 3회 이상 (로컬 백업 환경 포함) |
| 최종 발표자료 · 최종 보고서 |
| ERD·구성도·화면흐름도 산출 |
| README 정비, 코드 정리 |

---

## 2. 도메인별 TODO 체크리스트

### 🗄️ Database (김재표)

**✅ 완료**
- [x] SQLAlchemy 모델 10개 파일 + 테이블 12개
- [x] Alembic 세팅 + 리비전 3개 (테이블 rename, 타입 변경 포함)
- [x] 종목마스터 시드 (코스피 30종목)
- [x] PostgreSQL 호스팅 (Supabase 추정)

**🔴 최우선**
- [ ] 모델(`Numeric`) ↔ 마이그레이션(`Integer`) 타입 드리프트 해소 (`04` D-4)
- [ ] `autogenerate` diff 확인 → 의도치 않은 변경 없는지
- [ ] 안전장치 리비전: CHECK(수량>0, 잔고>=0) + `portfolio` UNIQUE + 인덱스 (`04` §3-1)
- [ ] `account.balance` 의미 정리 (`04` D-1)

**🟠 신규**
- [ ] `trades` 테이블 (`04` §4-1)
- [ ] `account_daily_snapshot` (`04` §4-2)
- [ ] `favorites` (`04` §4-3)
- [ ] 학습 도메인 6개 테이블 (`04` §4-4)
- [ ] `survey_question`/`survey_option`, `ai_news_summary`, `ai_coach_report` (`04` §4-5)
- [ ] 종목마스터 300종목 확대 + `market`/`is_active` 컬럼
- [ ] `market_calendar` 2026년 영업일 적재
- [ ] 타임존 전환 (`DateTime` → `TIMESTAMPTZ`) (`04` §3-3)
- [ ] 정합성 검증 쿼리 배치화 (`04` §5)
- [ ] `Database/Table` → `schema.dbml` 갱신 + **ERD 이미지 산출** (발표용)

### ⚙️ Backend (김동휘, 장성우)

**✅ 이미 완료된 항목** (`gp-mock-inv/backend`)
- [x] 프로젝트 스캐폴딩 (routers/services/schemas/models/utils)
- [x] `config.py` (pydantic-settings), `database.py`
- [x] `utils/security.py` (bcrypt 해시 + JWT), `utils/deps.py` (get_current_user)
- [x] SQLAlchemy 모델 10종 + Alembic 리비전 3개
- [x] `/api/users` (회원가입·로그인·내정보·성향저장)
- [x] `kis_service` (토큰 23시간 캐시 + 현재가 + 차트)
- [x] `/api/stocks` (검색·상세·현재가·차트)
- [x] `/api/trading/orders`, `/accounts`, `/portfolio`
- [x] `/api/community`, `/api/news`, `/api/ranking`, `/api/ai/survey`
- [x] Render 배포 + Swagger 문서

**🔴 최우선 수정** (`11_백엔드_코드리뷰.md`)
- [ ] **C-1** 체결가를 서버가 KIS 시세로 결정 (클라이언트 price 무검증 제거)
- [ ] **C-2** `price`/`quantity`에 `gt=0` + DB CHECK 제약
- [ ] **C-3** `account.balance` 정리 → 총자산 = 현금 + Σ(보유수량×현재가)
- [ ] **C-4** `with_for_update()` 로 계좌·포트폴리오 행 잠금
- [ ] **H-1** 모델(`Numeric`) ↔ 마이그레이션(`Integer`) 타입 일치
- [ ] **H-2** `httpx(verify=False)` 환경별 분리
- [ ] **H-3** 시세 캐시 (TTL 5초) + 유량 제한

**🟠 신규 개발**
- [ ] `trades`(체결) 테이블 + 실현손익 기록
- [ ] `fee_policy`(수수료·세금), `market_calendar`(장운영시간), `tick_size`
- [ ] 미체결 주문 개념 (`locked_cash` + 지정가 대기 + 스캐너)
- [ ] `/portfolio/summary` (총자산·수익률·MDD) + 일별 스냅샷 배치
- [ ] `/favorites` (관심종목)
- [ ] `/learn/*` (개념·퀴즈·용어·트리거)
- [ ] 호가·지수·랭킹(거래대금/급등락) KIS 연동
- [ ] `MarketDataProvider` LIVE/REPLAY/MOCK 폴백 ★ 시연 안전장치
- [ ] 리프레시 토큰 + 로그아웃
- [ ] 통일 에러 응답(`code`/`help_concept_id`)
- [ ] 체결 단위 테스트 12종 (`07` 문서 §9)

### 💻 Frontend (이시영)
- [ ] Tailwind CDN → 정식 설치 (`@tailwindcss/vite` 또는 PostCSS)
- [ ] `index.css` 전역 토큰 (색상/폰트/상승·하락 색상)
- [ ] `App.jsx` 라우팅 재구성 (중복 제거, 누락 3개 등록, 404, `/signup` 통일)
- [ ] `components/layout/` (Layout, Header, Nav, Footer) 추출
- [ ] `contexts/AuthContext.jsx` + `ProtectedRoute`
- [ ] `api/client.js` (env 기반 baseURL + 401 응답 인터셉터)
- [ ] 도메인별 api 모듈 8종
- [ ] Login / Signup 실제 폼 + 유효성 검사
- [ ] Dashboard 실데이터 연동 (지수/랭킹/뉴스/커뮤니티)
- [ ] **StockDetail 페이지 신규** (차트 + 호가 + 뉴스 + 주문 진입)
- [ ] Trading 페이지 (호가창/차트/주문폼/미체결 목록)
- [ ] Assets 페이지 (요약/보유종목/수익률 그래프/도넛)
- [ ] Favorites 페이지 신규
- [ ] Learn 페이지 신규 + `HelpIcon` 전역 배치
- [ ] Survey API 연동 (하드코딩 제거)
- [ ] Community 페이지 (COULD)
- [ ] MyPage / 404 페이지
- [ ] 로딩 스켈레톤 / 에러 / 빈 상태 컴포넌트
- [ ] 반응형 점검 (최소 1280px 데스크톱 우선, 태블릿 대응)
- [ ] `Assets.jsx`의 달러 자산 표시 처리 결정 (제거 또는 정식 기획)

### 🤖 AI (김진표, 권영준)

**✅ 완료**
- [x] 설문 답변 저장/조회 API (`POST/GET /api/ai/survey`)
- [x] 성향 결과 조회 API (`GET /api/ai/propensity`) — 단, 결과를 넣는 주체가 없음
- [x] **경제 용어 원고 60개 작성** (Google Docs PDF) ← 큰 자산

**🔴 LLM 없이 지금 할 수 있는 것 (먼저 하세요)**
- [ ] **Google Docs 원고 용어 60개 → `glossary_term` 시드 SQL 변환** (새로 쓰지 말 것)
- [ ] 설문 6문항 × 4선택지 실제 내용 확정 → `survey_question`/`survey_option` 시드
- [ ] **규칙기반 성향 점수 산출기** → `ai_propensity_advice` INSERT (폴백이자 검증 기준)
- [ ] `learn_concept` 15개 (11개는 원고 재활용, 4개만 신규 작성)

**🟠 LLM 연동**
- [ ] LLM 제공사·모델 확정 + 키 발급 + 월 예산 설정
- [ ] `services/ai/llm_client.py` (JSON 강제 출력, 재시도, 타임아웃, 비용 카운터)
- [ ] 성향 분석 프롬프트 연결 + 폴백 검증
- [ ] 뉴스 파이프라인 재구성 (검색 API → 요약 → 감성 → DB 저장)
- [ ] 코치 지표 계산기 (승률/회전율/집중도/손절률) — `trades` 테이블 필요
- [ ] AI 코치 프롬프트 + 리포트 API
- [ ] 학습 트리거 규칙 5종 (`08` §6-5)
- [ ] 퀴즈 30문항
- [ ] AI 고지 문구 전 화면 적용

**🔧 구조 결정**
- [ ] 외부 AI 서버(`:8001`) → **`services/ai/` 내부 모듈로 통합** 여부 확정 (`08` §1-4)
- [ ] `/api/ai/recommend`(종목 추천) 삭제 또는 "학습 주제 추천"으로 변경 (`08` §2)

### 🛠️ 공통/인프라 (팀장 + 전원)
- [ ] **`gp-mock-inv/` git 커밋 (최우선 — 현재 버전관리 밖)**
- [ ] 저장소 통합: 루트 `backend/`(빈 껍데기) 정리
- [ ] `.gitignore`에 `.env` 계열 추가
- [ ] `.env.example` (backend, frontend)
- [ ] 브랜치 전략: `main` ← `develop` ← `feat/*` (PR 리뷰 1인 필수)
- [ ] 커밋 컨벤션 (`feat:`, `fix:`, `docs:`, `refactor:`)
- [ ] GitHub Issues + Projects 보드
- [ ] `docker-compose.yml` (로컬 DB — 시연 백업 환경용)
- [ ] Render 콜드스타트 대응 (워밍업 / 로컬 백업)
- [ ] `requirements.txt` 정리 (`requests`·`python-dotenv` 제거, `pytest` 추가)
- [ ] (선택) GitHub Actions: lint + test
- [ ] 루트 `README.md` 재작성 (실행 방법 포함)
- [x] 백엔드 배포 (Render) ✅
- [ ] **프론트엔드 배포** (Vercel/Netlify 등) 결정

---

## 3. Definition of Done (DoD)

작업을 "완료"로 옮기기 전 아래를 만족해야 합니다.

**백엔드**
- [ ] Swagger에서 정상 응답 확인
- [ ] **클라이언트가 보낸 금액을 신뢰하지 않는지 확인**
- [ ] 잔고·수량을 바꾸는 로직은 `with_for_update()` 적용
- [ ] 에러 케이스 응답이 `03` 문서 규약 형식
- [ ] 금액 계산은 `Decimal` 사용
- [ ] DB 변경은 Alembic 리비전으로 반영
- [ ] 핵심 로직에 단위 테스트 1개 이상

**프론트엔드**
- [ ] 로딩 / 에러 / 빈 데이터 상태를 모두 처리
- [ ] 하드코딩 목업 데이터 제거
- [ ] 콘솔 에러/경고 없음
- [ ] 로그인 필요 화면은 비로그인 접근 시 리다이렉트

---

## 4. 리스크 등록부

| # | 리스크 | 확률 | 영향 | 대응 |
|---|---|:--:|:--:|---|
| R1 | **거래 결함을 안 고친 채 시연** (무한 수익·랭킹 역순 지적) | **높음** | **치명** | Week 1-2에 C-1~C-4 최우선 처리. 수정 전 시연 금지 |
| R1-b | **React 프론트 연동이 끝나지 않아 demo.html로 발표** | 중 | 높음 | `demo.html`을 정답지로 삼아 W3-4에 집중. 최악의 경우 백업 데모로 인정 가능 |
| R2 | ~~KIS API 승인 지연~~ → **해소됨** (연동 완료) | ─ | ─ | ✅ |
| R3 | KIS 유량 제한 초과로 차단 | 중 | 높음 | 캐시 + 레이트리미터 (`06` 문서 §5) |
| R4 | 발표가 휴장일/장외 시간 | **높음** | 높음 | `REPLAY`/`MOCK` 모드 필수 구현 |
| R5 | 체결 엔진 잔고 불일치 버그 | 중 | 높음 | 트랜잭션 + `FOR UPDATE` + 정합성 검증 배치 + 테스트 12종 |
| R6 | LLM 비용 초과 | 중 | 중 | 캐싱, 월 예산 상한, 폴백 |
| R7 | 뉴스 크롤링 적법성 문제 | 중 | 중 | 공식 API 우선, 요약+링크 원칙 |
| R8 | 6인 동시 커밋 충돌 | 높음 | 중 | 브랜치 전략 + 파일 담당제 |
| R9 | 학습 시스템 미착수로 발표 핵심기능 누락 | **높음** | **높음** | 용어 원고 60개가 이미 있으므로 시드 변환부터 즉시 시작 |
| R10 | 시연 중 서버 다운 | 중 | 치명 | 리허설 3회 + 로컬 백업 환경 |
| R11 | **Render 콜드스타트**(첫 요청 15초+)로 시연 첫 화면이 멈춤 | **높음** | 높음 | 발표 5분 전 워밍업 + 로컬 백업 (`10` 문서 C-6) |
| R12 | **`gp-mock-inv/` 미커밋 상태에서 PC 고장** | 낮음 | **치명** | 오늘 커밋 |

---

## 5. 주간 운영 규칙 (권장)

- **월요일 30분 스탠드업**: 지난주 완료 / 이번주 목표 / 막힌 것
- **금요일 통합 데이의**: `develop` 브랜치에서 전원이 한 번 돌려보기
- **10/15 중간 점검**: MUST 완료 여부 판정 → COULD 드롭 결정
- 막히면 **2시간 규칙**: 혼자 2시간 이상 막히면 반드시 팀에 공유
